name: CI Workflow

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Verify Calculator Files
      run: |
        echo "Testing if index.html exists..."
        test -f index.html
